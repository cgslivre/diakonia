var app = angular.module('musicaEscalaRecord', ['ngMessages','ngSanitize','remoteValidation'])
  .config(['$interpolateProvider', function ($interpolateProvider) {
      $interpolateProvider.startSymbol('<%');
      $interpolateProvider.endSymbol('%>');
}]);

app.controller('musicaEventoCreateCtrl', ['$scope', '$http', '$location',
  function ($scope, $http,$location) {
    $scope.evento = {};
    $scope.evento.titulo = "Encontro Geral";

  }
]);

//# sourceMappingURL=app-musica-staff-module.min.js.map
